specVersion: "v1"
serviceId: "predictive-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "predictive-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
